import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Vercel deploys Next.js natively — no extra config needed
  // Set output: "standalone" for Docker / self-hosted Node deployments
};

export default nextConfig;
