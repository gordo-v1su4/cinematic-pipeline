[project]
name = "cinematic-pipeline-backend"
version = "0.1.0"
description = "Backend API for the cinematic commercial pipeline"
requires-python = ">=3.11"
dependencies = [
    "robyn>=0.63.0",
    "httpx>=0.27.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
